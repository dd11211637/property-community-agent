name: property-community-agent

x-backend-environment: &backend-environment
  ENV: demo
  DEBUG: "false"
  DATABASE_URL: postgresql+psycopg://property_agent:demo-password@postgres:5432/property_agent_demo
  JWT_SECRET: demo-only-secret-change-before-production-32chars
  JWT_ALGORITHM: HS256
  JWT_EXPIRE_HOURS: "8"
  LOGIN_FAILURE_LIMIT: "5"
  LOGIN_FAILURE_WINDOW_MINUTES: "15"
  LOGIN_LOCK_MINUTES: "15"
  TRUSTED_PROXY_CIDRS: "172.16.0.0/12"
  LOG_LEVEL: INFO
  SLOW_REQUEST_THRESHOLD_MS: "1000"
  DEEPSEEK_BASE_URL: https://api.deepseek.com
  DEEPSEEK_MODEL: deepseek-v4-flash
  DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
  DEEPSEEK_CONNECT_TIMEOUT_SECONDS: "3"
  DEEPSEEK_READ_TIMEOUT_SECONDS: "12"
  DEEPSEEK_TOTAL_TIMEOUT_SECONDS: "6"
  MEMORY_EMBEDDING_BASE_URL: ${MEMORY_EMBEDDING_BASE_URL:-https://api.openai.com/v1}
  MEMORY_EMBEDDING_API_KEY: ${MEMORY_EMBEDDING_API_KEY:-}
  MEMORY_EMBEDDING_MODEL: ${MEMORY_EMBEDDING_MODEL:-text-embedding-3-small}
  MEMORY_EMBEDDING_VERSION: ${MEMORY_EMBEDDING_VERSION:-1}
  MEMORY_EMBEDDING_DIMENSIONS: "1536"
  RELEASE_SHA: ${RELEASE_SHA:-}
  DEPLOYMENT_ENVIRONMENT: ${DEPLOYMENT_ENVIRONMENT:-development}
  CERTIFICATION_WRITE_ENABLED: ${CERTIFICATION_WRITE_ENABLED:-false}

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: property_agent_demo
      POSTGRES_USER: property_agent
      POSTGRES_PASSWORD: demo-password
    ports:
      - "5432:5432"
    volumes:
      - property_agent_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U property_agent -d property_agent_demo"]
      interval: 3s
      timeout: 3s
      retries: 20

  migrate:
    build:
      context: .
      dockerfile: infra/backend.Dockerfile
    environment: *backend-environment
    command: ["alembic", "upgrade", "head"]
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  langgraph-setup:
    build:
      context: .
      dockerfile: infra/backend.Dockerfile
    environment: *backend-environment
    command: ["python", "scripts/setup_langgraph_checkpointer.py"]
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: "no"

  seed:
    build:
      context: .
      dockerfile: infra/backend.Dockerfile
      target: demo-support
    environment: *backend-environment
    command: ["python", "-m", "testing.seeds.seed_demo"]
    depends_on:
      langgraph-setup:
        condition: service_completed_successfully
    restart: "no"

  backend:
    build:
      context: .
      dockerfile: infra/backend.Dockerfile
    environment: *backend-environment
    ports:
      - "8000:8000"
    depends_on:
      seed:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/ready', timeout=2)",
        ]
      interval: 5s
      timeout: 3s
      retries: 20

  frontend:
    build:
      context: .
      dockerfile: infra/frontend.Dockerfile
    ports:
      - "5173:80"
    depends_on:
      backend:
        condition: service_healthy

  reset:
    profiles: ["tools"]
    build:
      context: .
      dockerfile: infra/backend.Dockerfile
      target: demo-support
    environment: *backend-environment
    command: ["python", "-m", "testing.reset.reset_demo", "--yes"]
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  postgres-test:
    profiles: ["testing"]
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: property_agent_test
      POSTGRES_USER: property_agent
      POSTGRES_PASSWORD: test-password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U property_agent -d property_agent_test"]
      interval: 3s
      timeout: 3s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data

  postgres-tests:
    profiles: ["testing"]
    build:
      context: .
      dockerfile: infra/backend.Dockerfile
      target: test
    environment:
      TEST_POSTGRES_URL: postgresql+psycopg://property_agent:test-password@postgres-test:5432/property_agent_test
      DATABASE_URL: postgresql+psycopg://property_agent:test-password@postgres-test:5432/property_agent_test
    command: ["python", "-m", "pytest", "-rs"]
    depends_on:
      postgres-test:
        condition: service_healthy
    restart: "no"

volumes:
  property_agent_postgres:
